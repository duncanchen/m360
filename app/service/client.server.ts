export const clientConfig = {
  client_id: process.env["client_id"] || "client_id",
  client_secret: process.env["client_secret"] || "client_secret",
}

export const authBaseUrl = process.env["authBaseUrl"] || "https://auth.mt360.io"

